<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.banzo.inaction.inaction">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".Login" /> -->
        <activity
            android:name=".MeusAlunos"
            android:label="Meus Alunos" />
        <activity android:name=".CadastroAvaliador" />
        <activity android:name=".Perfil" />
        <activity android:name=".ResetSenha" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CadastroAluno"
            android:label="Cadastro" />
        <activity android:name=".Avaliar"
            android:label="Avaliar"></activity>
    </application>

</manifest>